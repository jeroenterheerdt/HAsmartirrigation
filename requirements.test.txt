# Testing requirements for Smart Irrigation project

# Core component requirements
-r custom_components/smart_irrigation/requirements.txt

# Component-specific dependency
aiofiles

# Home Assistant and testing framework.
#
# pytest-homeassistant-custom-component pins the whole test stack to the set
# that matches the Home Assistant version it targets: pytest, pytest-asyncio,
# pytest-aiohttp, pytest-cov, freezegun, respx and syrupy are all requirements
# of it. Pinning them here as well only gives pip room to pick a combination
# that cannot be satisfied, which is how the install step broke outright when a
# floor was raised on one of them (#740). Let it drive.
homeassistant>=2026.6.4
pytest-homeassistant-custom-component>=0.13.342
